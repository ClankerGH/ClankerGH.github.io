Create a single self-contained SVG illustration of a rain-soaked solarpunk botanical observatory at blue hour.

The composition should show a large glass geodesic dome integrated into a futuristic research complex beside a lake or broad river. Through the transparent dome, show vegetation, structural supports, catwalks, and warm interior lighting. Include wet foreground pavement with convincing puddle reflections, a small autonomous tram crossing a bridge, distant mountains, atmospheric mist, clouds, a crescent moon, stars, shoreline vegetation, and a subtle distant city.

The artwork should demonstrate sophisticated native SVG construction.

Requirements:

- One valid standalone SVG file.
- viewBox="0 0 1600 1000".
- No raster images.
- No base64 images.
- No external assets.
- No image-generation tools.
- No JavaScript.
- All visual content must be constructed from native SVG.
- Use paths and Bézier curves for substantial portions of the artwork rather than relying almost entirely on primitive rectangles/circles.
- Use both linear and radial gradients.
- Use clipping paths.
- Use at least one mask.
- Use reusable symbols or <use> elements.
- Use transforms.
- Use opacity/transparency to depict glass, water, or atmosphere.
- Use SVG filters meaningfully, including at least blur and one procedural or compositing filter such as feTurbulence, feDisplacementMap, feBlend, or feColorMatrix.
- Include reflections in both glass and water/puddles.
- Use atmospheric perspective to distinguish foreground, middle ground, and background.
- Include some actual SVG text integrated naturally into the scene, such as understated building signage.
- Organize the SVG source into clearly named groups such as background, mountains, water, observatory, vegetation, lighting, foreground, and atmosphere.
- Keep repeated geometry efficient rather than manually duplicating every object.
- The final result should remain visually coherent and attractive at both 400px wide and 2400px wide.

Prioritize composition, depth, lighting, structural coherence, efficient source organization, and creative use of SVG capabilities.

DELIVERY REQUIREMENTS:

- Produce the complete SVG source and save it as one single downloadable file named:

  observatory-original.svg

- Do not return the SVG source as multiple Markdown or HTML code blocks.
- Do not return the SVG source inline in the chat unless file creation is impossible.
- If your generation process produces the SVG source in multiple textual chunks, concatenate those chunks in their original order before saving the file.
- "Concatenate" means joining all chunks end-to-end in their original order into one continuous SVG document.
- When concatenating, preserve the generated SVG verbatim. Do not redesign, regenerate, repair, optimize, summarize, or otherwise alter it.
- If Markdown code-fence markers exist around generated chunks, remove only those fence markers before concatenation.
- The finished file must begin with the original opening SVG/XML content and end with the original closing </svg> element.
- Return the downloadable file only.
- Do not include commentary, explanation, a summary, or a second copy of the SVG source.
